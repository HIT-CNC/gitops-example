apiVersion: v1
kind: Service
metadata:
  labels:
    app: flask-blog
  name: flask-blog
  namespace: default
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: flask-blog
  sessionAffinity: None
  type: NodePort
status:
  loadBalancer: {}
